import { PropertysumPipe } from './propertysum.pipe';

describe('PropertysumPipe', () => {
  it('create an instance', () => {
    const pipe = new PropertysumPipe();
    expect(pipe).toBeTruthy();
  });
});
