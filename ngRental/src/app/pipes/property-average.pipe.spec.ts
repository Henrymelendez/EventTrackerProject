import { PropertyAveragePipe } from './property-average.pipe';

describe('PropertyAveragePipe', () => {
  it('create an instance', () => {
    const pipe = new PropertyAveragePipe();
    expect(pipe).toBeTruthy();
  });
});
